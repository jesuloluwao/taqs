# Ralph Progress Log
Started: Wed Feb 25 10:30:23 WAT 2026
---

## Codebase Patterns
- Convex indexes: use camelCase names (by_userId, by_entityId) — NOT snake_case (by_user_id was wrong on old schema)
- When updating Convex schema indexes, grep all .ts files for old index names (userMutations.ts had a stale reference)
- Stub out pages that call removed/changed mutations — avoids TS errors during schema overhaul iterations
- npm (not pnpm) is the package manager for this project: `npm run build`
- Build command: `npm run build` from project root (runs web build via workspace)

- Convex Actions: add `"use node";` at top of file to use Node.js APIs/npm packages (mutations cannot be in "use node" files — keep them separate)
- Cross-module internal calls from actions: use `(internal as any).moduleName.fnName` since _generated/api.d.ts isn't regenerated during dev
- PDF parsing in Convex: install `pdf-parse` in convex/package.json; use dynamic import inside action handler for CJS compatibility
- `makeFunctionReference` exists in convex/server but type assertions on `internal` are simpler for cross-module action→mutation calls

---

## 2026-02-25 - US-015
- Rewrote `transactions` table in convex/schema.ts with full PRD-1 schema (20+ fields including entityId, direction, type expanded, amountNgn, fxRate, whtDeducted, deductiblePercent, taxYear, reviewedByUser, etc.)
- Added `importJobs` table tracking import lifecycle (pending→processing→complete/failed)
- Added required indexes: by_entityId_taxYear, by_entityId_date, by_entityId_type on transactions; by_entityId, by_connectedAccountId, by_userId on importJobs
- Updated convex/mutations.ts: createTransaction (new schema), updateTransaction (new), deleteTransaction, createImportJob, updateImportJob, migrateLegacyTransactions (migration helper)
- Updated convex/queries.ts: listTransactions (now entity-scoped with entityId required), getDashboardSummary (entity+taxYear, uses amountNgn), getTransaction, listImportJobs, getImportJob
- Updated packages/shared/src/types.ts: expanded TransactionType, new TransactionDirection, ImportJobSource, ImportJobStatus, TransactionData (full PRD-1), ImportJobData
- Updated packages/shared/src/schemas.ts: full transactionSchema, importJobSchema, importJobUpdateSchema with new Zod enums
- Fixed convex/userMutations.ts: stale `by_user_id` index → `by_userId`
- Stubbed Income.tsx and Expenses.tsx (they used old schema; full transaction UI comes in later stories)
- **Learnings for future iterations:**
  - Always grep for all index name references when renaming schema indexes
  - The old transactions schema (amountKobo, transactionDate, category as string) is completely gone — future stories must use amount, date, categoryId
  - listTransactions now requires entityId — any page using it must pass the active entity
  - migrateLegacyTransactions mutation is available to clean up pre-schema data via the API
  - pnpm not available — use npm
---

## 2026-02-25 - US-016
- Created `convex/transactions.ts` with full namespace: `list` (paginated, multi-filter, sortable, category names resolved), `get`, `getUncategorised`, `manualCreate`, `update`, `remove`, `bulkCategorise`, `bulkDelete`, `initiateImport`, `batchUpsert` (with dedup)
- Created `convex/importJobs.ts` with `get` and `list` queries
- `list` uses offset/limit pagination (JS-level slice after collecting from best-matching index)
- `batchUpsert` deduplicates on (entityId, date, amount, description) + externalRef; updates importJob status to complete with stats
- `manualCreate` computes `amountNgn` from fxRate if non-NGN currency; defaults to 1:1 if fxRate not provided
- `bulkCategorise` resolves `isDeductible` from category's `isDeductibleDefault`
- Build: `npm run build` passes (tsc + vite)
- **Learnings for future iterations:**
  - Convex namespace pattern: create a file named `transactions.ts`, export named functions — they become `transactions:list`, `transactions:get`, etc. on the client
  - Convex does not support offset-based pagination natively; using JS-level slice after `.collect()` is acceptable for small-to-medium datasets
  - For dedup in `batchUpsert`, filtering by `by_entityId_date` index first then JS filter on amount+description is efficient
  - `delete` is a reserved word in JS — use `remove` for the delete mutation export (same pattern as `entityCrud.ts`)
---

## 2026-02-25 - US-017
- Created `convex/importHelpers.ts`: internal mutations for importJob lifecycle management
  (getJob query, setJobProcessing, setJobComplete, setJobFailed) and batchInsert (dedup logic)
- Created `convex/importPipeline.ts`: `processImport` action with `"use node";`
- CSV parser: handles standard 5-column + Nigerian bank variations (GTBank, Zenith, Access, debit/credit)
  - Header detection: matches narration/value date/remarks/debit amount headers to identify bank format
  - Amount parsing: strips ₦ symbol + commas, converts float to kobo (×100)
  - Date parsing: supports DD/MM/YYYY, DD/MM/YY, YYYY-MM-DD, DD Mon YYYY
- PDF parser: uses pdf-parse (installed in convex/package.json) for text extraction
  - Bank detection from PDF text content (GTBank, Zenith, Access, generic fallback)
  - GTBank PDF: regex matching rows starting with date + narration + debit/credit amounts
  - Generic fallback: dateRe + amountRe pattern matching with CR/DR direction inference
- Format auto-detection: magic bytes check (%PDF = 0x25504446) + source field
- externalRef: SHA-256 hash of date|description|amount|direction (truncated 32 chars)
- Error handling: parse errors → setJobFailed; insert errors → setJobFailed with partial stats
- Partial parse resilience: try/catch per row — successful rows imported even if others fail
- `npm run build` passes (tsc + vite)
- **Learnings for future iterations:**
  - Convex `"use node"` files: ONLY export `action` functions — mutations/queries can't be in the same file
  - Internal function cross-file calls from actions: `(internal as any).module.fn` bypasses stale generated types
  - `pdf-parse` is CJS — use `const mod = await import('pdf-parse'); const fn = mod.default ?? mod;` pattern
  - convex/package.json deps need `npm install` run in `/convex` directory separately
  - Amount storage convention: ALL amounts stored in smallest unit (kobo = Naira × 100) for integer math
---

## 2026-02-25 - US-018
- Built full Transaction List UI at `/app/transactions`
- Extended `convex/transactions.ts` list query: added `direction` filter arg (credit/debit) and returns `categoryColor`/`categoryIcon` from resolved category
- `apps/web/src/pages/Transactions.tsx`: completely rewritten with:
  - Page header with 'Transactions' title + count subtitle + 'Import' button
  - Filter chips: All | Income | Expenses | Uncategorised | This Month | This Quarter | Custom Range
  - Custom Range picker (date inputs dropdown anchored to chips bar)
  - Debounced search bar (350ms) with clear button
  - Sort dropdown: date desc/asc, amount desc/asc, category A–Z/Z–A
  - Grouped by month with sticky section headers ('February 2026')
  - Transaction rows: direction icon (green/red), category color dot, description truncated, date, category label or 'Uncategorised' in warning color, amount (+green for credit / neutral for debit), foreign currency badge + original amount
  - Load more button (25 per page, increases limit by 25)
  - Footer showing total count when all loaded
  - Skeleton loading state (8 shimmer rows)
  - Empty state with illustration + contextual message + Import Now CTA or Clear filters link
- `npm run build` passes (tsc + vite)
- **Learnings for future iterations:**
  - `api.transactions.list` fails TypeScript — stale generated types. Use `(api as any).transactions.list` and cast result to a local interface
  - Same pattern as internal action calls: `(api as any).module.fn` for any module not in the generated api.d.ts
  - Define a local `TransactionRow` interface for typing query results when generated types are stale
  - Filter chips for Income/Expenses use `direction` (credit/debit) not `type` — more intuitive for users
  - Limit-based pagination (increasing limit state) works well with Convex reactive queries — no separate accumulation needed
---

## 2026-02-25 - US-019
- Created `apps/web/src/pages/TransactionDetail.tsx`: full transaction detail/edit page at `/app/transactions/:id`
  - Hero summary card: direction icon, description, naira amount, date, category badge, foreign currency badge
  - View mode: all fields shown in labeled rows (date, amount, FX rate, currency, direction, type, category, source, invoice, taxYear)
  - Tax information section: isDeductible toggle, deductiblePercent, whtDeducted, whtRate
  - Notes section with edit textarea
  - Edit mode: inline editable inputs for description, category (dropdown), type, isDeductible toggle, deductiblePercent, whtDeducted, notes
  - Save/Cancel header buttons with dirty form tracking
  - Discard Changes dialog shown when cancelling with unsaved edits
  - 'Mark as Personal' one-tap action button (type=personal_expense, isDeductible=false)
  - Delete Transaction button with confirmation dialog (permanent delete → navigate back to list)
  - Success/error toasts on all actions
  - Loading skeleton and 404 not-found state
- Created `apps/web/src/components/ManualTransactionModal.tsx`: slide-up modal for manual transaction entry
  - Fields: date, description, amount, currency, direction, FX rate (shown for non-NGN), computed NGN equivalent, category dropdown, type, isDeductible, deductiblePercent, whtDeducted, whtRate, notes
  - Auto-sets isDeductible from category.isDeductibleDefault when category is selected
  - Converts user-entered Naira amounts to kobo on submit
  - Inline validation errors for required fields and range checks
  - Closes on Escape key or backdrop click
- Updated `convex/transactions.ts`: `get` query now returns `categoryColor` and `categoryIcon` alongside `categoryName`
- Updated `apps/web/src/App.tsx`: added `/app/transactions/:id` route → `TransactionDetail`
- Updated `apps/web/src/pages/Transactions.tsx`:
  - Added `+` (Add) button next to Import button in header
  - Transaction rows are now clickable (navigate to `/app/transactions/:id`)
  - Added `ManualTransactionModal` rendered when showManualModal is true
- `npm run build` passes (tsc + vite)
- **Learnings for future iterations:**
  - For modal/overlay components: use `fixed inset-0 z-50` + backdrop div + animate-slide-up on panel — consistent with existing design
  - Amount input convention: users enter in currency units (Naira), multiply by 100 to store in kobo for all monetary fields (amount, whtDeducted)
  - `useParams<{ id: string }>()` returns string; pass directly as `Id<'transactions'>` cast to Convex — runtime validates the ID format
  - For edit forms, keeping `isDirty` state and showing a discard dialog before cancelling is good UX (implemented via DiscardChangesDialog)
  - When a category is selected in an edit form, auto-apply `isDeductibleDefault` from that category
---

## 2026-02-25 - US-020
- Created `apps/web/src/pages/ImportTransactions.tsx`: full Import Transactions screen at `/app/import`
  - Method tabs: Upload Statement | Connect Bank (placeholder) | Connect Fintech (placeholder) | Manual Entry
  - Upload tab with drag-and-drop zone (web); accepts PDF and CSV files
  - State machine: idle → uploading (progress bar) → processing (indeterminate bar + "Processing your statement…") → complete/error
  - Upload flow: generateUploadUrl → fetch POST to Convex Storage → initiateImport mutation → processImport action (fire-and-forget) → subscribe to importJobs.get via ImportJobWatcher component
  - Complete state: results summary card showing "X transactions imported, Y duplicates skipped" with CTA to view batch or go to transactions
  - Error state: error message with "Try again" button (resets to idle)
  - Duplicate explanation info box (always visible on idle; conditional on complete)
  - ImportJobWatcher uses useQuery on importJobs.get for real-time Convex reactivity
  - Connect Bank / Connect Fintech tabs: placeholder with "Coming soon" badge
  - Manual Entry tab: redirects to /app/transactions
- Updated `apps/web/src/pages/Transactions.tsx`: Import button now navigates to `/app/import` (removed toast placeholder)
- Updated `apps/web/src/App.tsx`: added `/app/import` route → `ImportTransactions`
- `npm run build` passes (tsc + vite)
- **Learnings for future iterations:**
  - Convex Storage upload: POST to generateUploadUrl result with Content-Type header; response JSON has `storageId`
  - `useAction` from convex/react is needed for calling Convex actions (vs `useMutation` for mutations)
  - For real-time job status: subscribe via useQuery on importJobs.get; use a ref to avoid double-notifying on re-renders
  - `CloudUpload` is not exported from lucide-react — use `UploadCloud` instead
  - Fire-and-forget pattern for actions: call `.catch()` to handle errors but don't await in main flow; job watcher handles success/failure
---

## 2026-02-25 - US-021
- Created `apps/web/src/components/CategoryPickerModal.tsx`: reusable category picker
  - Fetches `api.categories.listAll`; searchable; grouped by Income/Business/Personal/Transfer
  - Exports `CategoryOption` interface for consuming components
  - Shows "Deductible" badge on categories with `isDeductibleDefault: true`
- Created `apps/web/src/pages/Triage.tsx`: card-based triage at `/app/triage`
  - Captures uncategorised queue once on load (prevents reactive list shifting)
  - Progress bar (% reviewed) + "N of M" counter
  - Actions: Change category (picker → apply + advance), Mark as Personal, Skip
  - All-done state + empty state (0 uncategorised)
- Updated `apps/web/src/pages/Transactions.tsx`: bulk select mode
  - "Select" toggle in header; checkboxes on rows in bulk mode
  - "Select all" bar selects visible page only; selected rows highlighted
  - Fixed bottom action bar (`md:left-64` for sidebar offset): Categorise + Delete
  - Bulk Categorise: CategoryPickerModal → `bulkCategorise` mutation
  - Bulk Delete: confirmation dialog → `bulkDelete` mutation
- Updated `apps/web/src/App.tsx`: added `/app/triage` route
- `npm run build` passes
- **Learnings for future iterations:**
  - `api.categories.listAll` is in generated types (no `as any`); transactions still needs it
  - CategoryPickerModal sticky group headers: `sticky top-0` inside `overflow-y-auto flex-1`
  - Bottom bulk bar: `md:left-64` to clear desktop sidebar width
  - Triage queue: guard with `queue === null` in useEffect to freeze list on first load
  - `bulkCategorise` requires both `categoryId` AND `type` — pass `category.type` from picker
---

## 2026-02-25 - US-022
- Added `create`, `update`, `remove` mutations to `convex/categories.ts`
  - `remove` clears categoryId + sets type='uncategorised' on all affected transactions before deleting
- Created `convex/connectedAccounts.ts`: `list` query (entity-scoped, user-filtered), `add` mutation, `disconnect` mutation
- Created `apps/web/src/pages/Categories.tsx`: categories management at `/app/settings/categories`
  - Groups by type: Income, Business Expenses, Personal, Transfers
  - System categories (isSystem=true) show lock badge, no edit/delete actions
  - Custom categories show edit/delete actions on hover
  - Create modal: name, type selector, deductible toggle (business only), icon picker (emoji), color picker
  - Edit modal: same form pre-populated (type locked in edit mode)
  - Delete confirmation: warns transactions will move to uncategorised
- Created `apps/web/src/pages/ConnectedAccounts.tsx`: accounts screen at `/app/settings/accounts`
  - Empty state: "No accounts linked yet" + "Add Account" CTA
  - AccountCard shows: provider icon, name, status badge, currency, last synced relative time
  - Add Account modal: accountName, provider (statement_upload | manual), currency
  - Disconnect confirmation dialog; preserves existing transactions
- Updated `apps/web/src/App.tsx`: added routes for `/app/settings/categories` and `/app/settings/accounts`
- Updated `apps/web/src/pages/Settings.tsx`: added Categories and Connected Accounts link cards
- `npm run build` passes (tsc + vite)
- **Learnings for future iterations:**
  - Connected accounts module uses `(api as any).connectedAccounts.*` pattern (new module, not in generated types)
  - For categories `remove` mutation, must clear categoryId field on transactions — use `undefined` not `null` for optional Convex fields
  - Emoji icon + hex color picker combo works well for category customisation UX
  - Account status badges (active/expired/error/disconnected) use distinct color schemes for quick visual scanning
---
